AJAX (asyncronous javaScript and XML) mais on devrait plutot l'appeller AJAJ 

dans le fichier ou l'on place 'echo json encode();'  surtout supprimer ou mettre en commentaire tous les autres echo avant, cela interfere. il ne doit y avoir qu'un seul 'echo' celui de json_encode();

ressources:

tuto: graphicart - tchat 